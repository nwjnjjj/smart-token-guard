{name: publish-mcp, on: {workflow_dispatch: {}, release: {types: [published]}}, permissions: {id-token: write, contents: read}, jobs: {publish: {runs-on: ubuntu-latest, steps: [{uses: actions/checkout@v4}, {run: 'curl -sL https://github.com/modelcontextprotocol/registry/releases/download/v1.8.1/mcp-publisher_linux_amd64.tar.gz | tar xz mcp-publisher && ./mcp-publisher login github-oidc && ./mcp-publisher publish'}]}}}
